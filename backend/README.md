# Travell Backend

Simple Dart API server for the Flutter app.

## Run

```bash
cd backend
dart pub get
dart run bin/server.dart
```

The API runs on `http://localhost:8080` by default.

## Endpoints

- `GET /health`
- `GET /api/resident`
- `GET /api/flights`
- `GET /api/destination`
